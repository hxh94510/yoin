# 余音 YOIN

日系潮流聊天角色图鉴，基于「老田的嫡系们」的既有档案和聊天记录制作。

线上地址：<https://hxh94510.github.io/yoin/>

## 查看

直接用浏览器打开 `index.html` 即可使用全部核心功能，无需安装依赖、无需联网。

也可以在本目录运行 `node serve.cjs`，打开 `http://127.0.0.1:4173`。端口可通过 `YOIN_PORT` 环境变量修改；服务仅绑定本机。

## 功能

- 首页：原型角色、精选对白、测试入口。
- 首页精选区：每次进入 / 刷新从 11 位角色中随机抽取 4 位，避免连续原样重复。
- 角色图鉴：11 位角色，支持姓名、外号、标签搜索和高倾向筛选。
- 角色档案：完整性格描述、六维雷达、Tag、话题频次、经典原话及上下文、主羁绊。
- 羁绊网络：11 个可点选节点、55 对统计，支持发言转接 / 共同会话排序，5 个主羁绊与 5 个主题组合。
- 原声档案：23 条已核对发送人的引文，支持搜索与角色筛选。
- 测试实验室：两项 10 题情境测试，分别为聊天人格和「你的孩子最像谁」。支持返回改选、独立进度与结果保存、前三名、SVG 结果卡。
- 养成测试：10 个小学阶段育儿情景，结合兴趣 60% 与六维性格 40% 匹配；结果展示兴趣方向、三条作答回顾、原文依据与想象对白。
- 角色喜好：重新从原 CSV 核对发送人与相邻语境，区分明确偏好、本人体验、单次评价与讨论线索；档案及养成结果可展开查看日期和记录编号。
- 动漫选角台「制作一部什么动漫」：11 人不重复分配至 8 个位置，支持交换、随机、撤销和独立存档；从 500 个 IMDb 动画剧集条目中计算最佳匹配、两部候选，生成可下载的八人阵容海报。
- 自适应手机 / 平板 / 桌面，键盘操作，尊重系统减少动效设置。
- 循环 BGM「軽快なリズム」、动态唱片按键、独立音乐 / 音效开关及音量设置。
- Cuelume 的轻量交互音效覆盖悬停、选择、翻页、展开、结果和操作成功。

## 声音控制

首次打开默认尝试自动播放 BGM；如果浏览器禁止有声自动播放，会显示“轻触页面，开启音乐”，首次点击 / 轻触页面或按 Enter、空格时自动接续播放。不会绕过浏览器的音频权限。右下角唱片按键开启 / 关闭 BGM；音乐开关、音量与音效偏好保存在当前浏览器。手动关闭后，下次打开仍保持关闭。站内导航和切换后台标签页都会保留同一个播放位置；若浏览器或系统在后台暂停媒体，回到页面时会自动续播。

音乐默认音量 20%，交互音效 25%；音频设置中可独立关闭音效。音乐循环播放，切到后台标签页时继续播放；自动播放受限时等待交互，文件加载失败时按键显示重试状态，均不影响正常浏览和测试。

音效使用 [Cuelume 0.2.2](https://github.com/danielwh2/cuelume)，保留 MIT 许可证。模块已打包为本地 IIFE，直接打开 HTML 也可以使用，不依赖 CDN。音乐为用户提供的文件直接复制。

| 交互 | 音效 |
|---|---|
| 鼠标悬停导航 / 角色卡 | tick，库内 150ms 节流，触摸不触发 |
| 改选答案 / 筛选 / 羁绊分页 | toggle；重复选择不响 |
| 上一题 / 下一题 / 普通导航 | page |
| 进入角色 / 展开原话 | bloom；收起为 droplet |
| 开始测试 / 生成结果 | pulse / ready |
| 成功复制 / 生成下载卡 | success |
| 可恢复失败 | error |

系统减少动效时唱片和音柱静止；播放状态仍以文字显示。滑块拖动、打字、滚动与不可用按钮不连续发声。

## 数据与评分

六维顺序：追问、拆解、共鸣、玩梗、行动、精炼。原型数值为基于既有档案的人工设计参数，与发言条数等统计指标分开；不代表标准化心理量表。

网页「数据与设计说明」列明各维解释、10 分钟间隔会话切分、20 分钟主题分段、词簇统计限制，以及评分算法。八卦线索移除了单独的“喜欢”；统计仍是关键词候选，需结合上下文。

每题选项有一个六维向量，题目等权。每维以“各题四个选项等概率被选”的合成参照计算总分均值和标准差，将选择相对该参照的偏移映射到 11 个原型在该维的均值与标准差，最后截断至 0–100。这只是让题目参数与原型参数在同一尺度上比较，不是人群常模。

雷达图公式保持为 `分数 = clamp(原型均值 + (选项总分 − 合成均值) / 合成标准差 × 原型标准差, 0, 100)`。评分 v3 不再只按六维均方根距离排名：中间型原型容易因此吸收大量混合作答。

新排名先将角色原型按全员逐维均值、标准差中心化，取单位方向；选项六维按相同标准差缩放，投影到该方向。对每位角色，汇总所选选项的支持度，减去每题选项等概率被选时的预期支持度，再除以合成标准差。标准差按**完整选项的投影**计算，包含选项内部各维的相关性，不将相关维度当成独立证据。题目等权，不设角色配额，不使用随机兜底。

`证据 = (本次支持度 − 等概率参照均值) / 参照标准差`；展示指标为 `100 / (1 + exp(−证据 / 1.5))`，四舍五入。50 表示合成参照的平均支持度，不是人群百分位或身份概率。按未取整的证据排序；雷达图的线条距离不再单独决定名次。模型定义和独立的 `scoringVersion` 保存在 `tests.js`。

没有分析埋点、远程字体、外部图片、API 或服务端存储。测试进度与结果仅保存在当前浏览器；禁用本地存储时仍可完成当前页面中的测试。

### 养成测试的兴趣匹配

`interests.js` 保存 12 个兴趣方向、11 位角色的正向兴趣线索与经核对的原文。权重是编辑归纳，未提及不等于不喜欢，也不是关键词命中次数。复读、他人偏好、否定句和夸张安利中的游玩时长均不能直接作为本人喜好。

每个选项同时包含六维行为向量与兴趣标签。兴趣方向的展示分数仍为「所选标签权重之和 / 各题该标签可选的最大权重之和」。排名不再用原始余弦分直接与六维相加：改为选项兴趣标签与角色**正向**兴趣权重的点积，对每位角色分别扣除等概率作答下的均值、除以标准差，处理兴趣出现机会、标签组合与档案疏密带来的天然优势。缺失线索不被补成负面喜好。

`综合证据 = 兴趣证据 × 0.6 + 六维证据 × 0.4`，之后统一转为上述 0–100 指标；不是将两个已取整的展示分直接加权。聊天测试仅用六维证据。

结果保存完成时的答案快照；重答过程中仍可查看上一次完整结果，直到再次完成后更新。题目 schema 版本未变，因此本次保留已有进度，并从旧结果的完整答案重算，页面明确提示规则升级。很早期无答案的聊天摘要保留旧版算法并标注需要重测，不猜测缺失答案。养成场景、兴趣彩蛋和未来对白属于创作推演，不是儿童发展预测；成人聊天中的饮品偏好只类比原味与甜口，不作为儿童饮品推荐。

### 动漫选角的 500 部作品池

入口 `#game/anime-studio`，结果 `#anime-result`，可搜索的作品库 `#anime-library`。

来源为 [IMDb Anime 分类](https://www.imdb.com/interest/in0000027/)的全部 TV Series / TV Mini Series，限定至少 1,000 票，按用户评分降序取前 500 个条目。2026-09-22 通过公开页面逐批载入，筛选池当时为 1,159 个条目，500 个 IMDb ID 全部唯一。数据保留整理时的评分与展示票数；不调用实时 API，不需要密钥。

这是 **有筛选条件的快照，不是 IMDb 官方历史 Top 500 榜单**。遵循 IMDb 分类，包含部分非日本制作、独立季、重制与发行版本；不同 IMDb 条目仍可能是同一作品的不同版本。结果前三名按系列进行去重。中文名仅用于展示，始终保留 IMDb 原名和源条目链接。

`anime-catalog.js` 包含 16 类题材信号，来自公开剧情简介的词簇提取及编辑注记。站点不复制整段剧情简介、IMDb 海报或原作对白；也不宣称完成了 500 部作品的逐角色人工解读。工作源快照不进入网站或 ZIP。

基础契合 = 位置契合 60% + 兴趣题材 25% + 组合化学 15%。每个位置有不同的六维目标、注意维度与兴趣侧重，主角和反派权重略高；组合使用共同会话在各自伙伴中的相对位置及主反派风格反差。评分 v2 对每部作品分别计算固定 8,192 套不重复阵容的基础契合均值与标准差，以 `(本次基础契合 − 均值) / 标准差` 经上述逻辑函数得到相对适配，抑制对所有阵容都容易高分的“万能作品”。最终指标 = 相对适配 75% + 基础契合 25%，保留绝对适配作为约束，不仅追求罕见结果。

`anime-calibration.js` 是由 `node scripts/build-anime-calibration.cjs` 生成的离线校准数据，固定种子 731942，不使用真实访客数据。每部作品的预期得分与波动均参与校准，没有人工按片名调分、命中配额或运行时抽奖。模型参数指纹用于检查是否需要重新生成；修改规则时提升 `SCORING_VERSION` 并重建校准。使用 `--check` 可验证文件可复现。

IMDb 评分不加进匹配分；完全同分时沿用评分快照的榜序。相同选角得到相同结果，交换位置改变计算；预先缓存全部位置 / 人物比较，避免浏览器反复计算同一组特征。

选角与完成快照分别存为 `yoin:anime-studio` / `yoin:anime-result`，不触碰两项原测试。禁用存储时仍能在当前页面完成游戏。`VERSION` 只控制存档结构，`SCORING_VERSION` 控制算法；旧阵容本次保留、重新匹配并提示，作品快照版本不变。

### 评分分布回归（2026-09-22）

固定评估种子 20260922，与动漫校准种子不同。两项测试各 20,000 组独立等概率作答；动漫 5,000 套不重复阵容。同一批输入对照升级前后算法。以下仅是**合成覆盖检查，不是真实玩家分布，不追求所有结果均匀**。

| 玩法 | 最常见结果占比：旧 → 新 | 其他观察 |
| --- | --- | --- |
| 聊天人格 | 18.04% → 12.64% | 11 种结果均可达 |
| 孩子养成 | 23.46% → 12.74% | 前三合计 66.28% → 35.20%，11 种均可达 |
| 动漫选角 | 17.34% → 3.20% | 命中作品 172 → 339 / 500；300 次主反派互换中 259 次改变第一名 |

语义回归包含规则分析→腿哥、短句共鸣→杰宝、戏剧加梗→鱼子、视觉创作→浩仔、音乐舞台→雅导、外出美食→小豆、故事与感受→杰宝。它们检查选择方向仍有含义，不仅统计结果数量。

## 扩展测试

1. 在 `tests.js` 的 `window.YOIN_TESTS` 数组中增加配置，提供唯一 `id`、`version`、标题、描述、时长、`status: 'ready'` 与 `questions`。
2. 沿用六维顺序。每个问题包含 `title`、`tag`、`time`、`messages`（说话人/文本数组）、`prompt` 和 `options`。
3. 每个选项包含 `text` 与六个 0–100 的 `vector` 值。当前界面适配四个选项；所有问题应覆盖各个维度的差异。
4. 页面自动生成测试入口和对应路由；题目改变时提升 `version`，旧进度将自动失效。
5. 新测试如采用不同维度或不同结果体系，应单独实现评分与结果展示，不能直接复用当前角色匹配算法。

养成测试在 `parenting.js` 注册，提供独立的兴趣评分参数及结果展示。新增养成题必须同步提供 `interests` 和 `seed`；修改题目、兴趣权重或评分含义时提升该测试的 `version`。后续的未来测试卡片目前是编辑计划，不可点击。所有情境题为新创作，网页原文引用另有来源标识。

## 文件

回归检查均无需安装依赖：`node tests/scoring.test.cjs`（确定性、选项 / 原型顺序不变性、非法答案、证据单调性、语义路线）；`node tests/anime.test.cjs`（500 条 ID、唯一选角、交换、稳定排名、系列去重、旧存档、存储异常与导出）；`node scripts/audit-scoring.cjs`（上述固定样本分布）；`node scripts/build-anime-calibration.cjs --check`（校准可复现）。

- `index.html`：入口与全局导航。
- `style.css`：响应式视觉与动画。
- `app.js`：页面、筛选、关系网、测试流程、保存和导出。
- `data.js`：11 位角色、引文、羁绊与互动统计。
- `tests.js`：测试注册和评分器。
- `interests.js`：带原文证据的角色兴趣与正向匹配权重。
- `parenting.js`：育儿情景、选项、养成结果与兴趣证据展示。
- `anime.js` / `anime.css`：选角、评分、作品库、独立进度和 SVG 阵容海报。
- `anime-catalog.js`：500 个可追溯 IMDb 条目的离线快照与题材信号。
- `anime-calibration.js`：500 部作品各自的合成阵容得分参照。
- `audio.js` / `audio.css`：声音控制、偏好、语义音效和动态按键。
- `assets/audio/keikai-na-rhythm.mp3`：BGM 原文件。
- `vendor/cuelume/`：Cuelume 0.2.2 本地构建、MIT 许可证和来源说明。
- `serve.cjs`：可选的本地预览服务。
- `assets/avatars/`：用户提供的 11 张头像，按文件名与角色对应；原文件直接复制。结果卡使用同一图片的内嵌数据以支持离线导出。

原 DOCX、PDF 与羁绊 Markdown 保持独立。网页可以通过复制整个文件夹继续编辑或部署。
